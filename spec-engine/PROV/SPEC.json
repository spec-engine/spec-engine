{
  "key": "PROV",
  "owner": null,
  "updated": "2026-07-30",
  "scope": "The spec provenance matrix and the issue-link model, with issue_id an opaque external payload. Belongs here: the matrix rows and their sort and render; the role:ID parse into (req_id, issue_id, role); issue_id is projected only, never a key. Does not belong here: resolving an issue id to a title (TRK); requirement identity or routing. Boundary case: an issue id shaped like KEY-NNN stays an opaque string and is never resolved against the requirements table.",
  "grammar": "ears",
  "requirements": [
    {
      "id": "PROV-001",
      "status": "superseded",
      "statement": "spec provenance treats every issue_id as an opaque payload — never a PK, FK, UNIQUE, index, or JOIN key — so an issue link can never silently become an identity or routing construct.",
      "why": "If an issue id became a join or routing key, an issue-link collision would silently reroute a requirement's provenance to the wrong requirement — corrupting the audit trail the provenance matrix exists to preserve.",
      "supersedes": null,
      "supersededBy": "PROV-004",
      "relates": [],
      "livesIn": [
        "@packages/shared/src/schema.ts"
      ],
      "issues": [],
      "changedAtVersion": 1,
      "supersededAtVersion": 2,
      "aliases": [],
      "cites": []
    },
    {
      "id": "PROV-002",
      "status": "active",
      "statement": "When resolved issue data is served from an expired cache after a failed refresh, spec provenance shall label it stale in both text and JSON output.",
      "why": "An old tracker title rendered as current is a quiet lie — degraded data must say it is degraded.",
      "supersedes": null,
      "supersededBy": null,
      "relates": [],
      "livesIn": [
        "packages/engine/src/provenance/resolve.ts"
      ],
      "issues": [],
      "aliases": [],
      "cites": []
    },
    {
      "id": "PROV-003",
      "status": "active",
      "statement": "When an authoring command is run with an issue flag, the spec CLI shall record the ticket id as provenance on the entry — created on a new requirement, supersedes-via on a superseded predecessor, amends-via on an amendment — never as identity or routing.",
      "why": "The engine warned about missing supersedes-via provenance while offering no way to write it — provenance the tool demands but cannot record is provenance that never exists.",
      "supersedes": null,
      "supersededBy": null,
      "relates": [],
      "livesIn": [
        "packages/engine/src/commands/supersede.ts"
      ],
      "issues": [],
      "aliases": [],
      "cites": []
    },
    {
      "id": "PROV-004",
      "status": "active",
      "statement": "The spec CLI shall store and display an issue id verbatim as an opaque string; an issue id shall never be resolved against the requirements table, shall never route provenance between requirements, and colliding issue ids shall coexist without merging.",
      "why": "If an issue id became a join or routing key, an issue-link collision would silently reroute a requirement's provenance to the wrong requirement — corrupting the audit trail the provenance matrix exists to preserve.",
      "supersedes": null,
      "supersededBy": null,
      "relates": [],
      "livesIn": [
        "packages/shared/src/schema.ts"
      ],
      "issues": [],
      "changedAtVersion": 1,
      "aliases": [],
      "cites": []
    }
  ]
}
