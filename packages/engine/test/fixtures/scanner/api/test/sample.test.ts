// fixture: test code file under packages/engine/test/fixtures/scanner/api/test/
// Tagged with @spec FOO-001 unit so scanner tests can assert kind=verifies
// and level=unit (path contains both `/test/` and `.test.`).
// @spec FOO-001 unit
export const SAMPLE_TEST = "test/sample.test";
